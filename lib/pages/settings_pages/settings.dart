import 'package:flutter/material.dart';

class SettingsPages extends StatelessWidget {
  static const String routeName = "SettingsPages";

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
